@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Welcome";
}

<h2>Welcome</h2>

<p>HelloWorldController, Welcome View!</p>

@{
    String[] param = { "Google", "googlePage", "HelloWorld" };
}

@Html.ActionLink(param[0], param[1], param[2])

<ul>
    @for (int i = 0; i < ViewBag.NumTime; i++)
    {
        <li>@ViewBag.Message</li>
    }
</ul>
